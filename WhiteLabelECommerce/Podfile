pod 'SwiftLint'

target 'WhiteLabelECommerce (iOS)' do
  platform :ios, '15.0'
  use_frameworks!

  target 'WhiteLabelECommerceTests(iOS)' do
    inherit! :complete
  end
end

target 'WhiteLabelECommerce (macOS)' do
  platform :osx, '12.0'
  use_frameworks!
  target 'WhiteLabelECommerceTests(mac)' do
    inherit! :complete
  end
end
